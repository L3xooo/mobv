<androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/warning_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Image -->
    <ImageView
        android:id="@+id/location_icon"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/location_off"
        app:tint="@color/light_grey"
        android:contentDescription="Icon"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Warning text -->
    <TextView
        android:id="@+id/warning_text"
        style="@style/heading2"
        android:text="Location is Turned Off"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/location_icon"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Description text -->
    <TextView
        android:id="@+id/description_text"
        style="@style/description1"
        android:gravity="center"
        android:text="The user list feature requires knowing your location to function correctly."
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/warning_text"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Enable location button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/enableLocationButton"
        style="@style/primaryBtn"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Enable Location"
        app:layout_constraintTop_toBottomOf="@id/description_text"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
